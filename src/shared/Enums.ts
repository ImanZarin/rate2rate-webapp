
export enum Pages {
    signin = "/signin",
    user = "/user",
    home = "/"

}

export enum Languages {
    en = "English",
    fi = "Finnish",
}

export enum MyCookies {
    token = "access-token",
    lan = "language",
    time = "time-zone"
}